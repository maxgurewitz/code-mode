base_dir = ".tmp/.code-mode"

[servers.hello-world]
command = "node"
args = ["$HELLO_WORLD_SERVER_PATH"]

[servers.echo-env]
command = "node"
args = ["$ECHO_SERVER_PATH", "$ECHO_SERVER_FLAG"]
env = { TEST_SECRET = "$TEST_SECRET_SOURCE", BRACED_SECRET = "${BRACED_SECRET_SOURCE}" }
